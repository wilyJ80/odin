1. Start PostgreSQL service
2. Run database population script `./db/populate.js`

# TODO:

- [ ] Interesting to think in ways to avoid coupling. Currently, routes in views accept arbitrary paths in <a> links. It can be useful to have some kind of "global" "router"
